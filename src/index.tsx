import ReactDOM from 'react-dom/client';


const root = ReactDOM.createRoot(
	document.getElementById('root') as Element
);

const element = <h1>Lorem, ipsum dolor.</h1>;
root.render(element);
